<!DOCTYPE html>
<html>
<head>
     <title>{{title}}</title>
    <meta charset="utf-8" />
</head>
<body>
 <h1>{{title}}</h1>
    {{#if emailsVisible}}
    
    <h3>Email:</h3>
    <ul>
        {{#each emails}}
        <li>{{this}}</li>
        {{/each}}
         </ul>
    {{/if}}
    <p>Phone: {{phone}}</p>
</body>
<html>
